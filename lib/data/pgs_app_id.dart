/// The Play Games Services app id, pinned so a test can check the manifest
/// carries it. It is the Firebase project's number — `firebase_config.dart`'s
/// sender id — and the number inside every id in `pgs_achievements.dart`: a
/// `CgkI…` id is a protobuf whose first field is this.
library;

const String pgsAppIdAndroid = '500974365483';
