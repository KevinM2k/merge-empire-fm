<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- **DECLARED HERE RATHER THAN INHERITED FROM THE PLUGINS.** The manifest
         merger would supply all three anyway — the ad SDK, the billing client
         and the network watcher each declare what they need — and that is
         exactly the problem: a plugin bump that drops one is invisible until a
         release build cannot reach the network or Play refuses the upload.
         INTERNET was in the DEBUG manifest only, which is the Flutter template's
         doing and means the shipped build's most basic permission came from
         whatever happened to be in `pubspec.yaml` that week.
         The same three the shipped app's own manifest carries. AD_ID is a Play
         requirement for anything that reads the advertising id on Android 13+,
         and BILLING is what makes the in-app products visible to the client. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <application
        android:label="Merge Empire Football Manager"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- AdMob crashes on start without this, rather than serving nothing.
             Pinned in lib/data/ad_app_ids.dart, where a test checks it. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-0386196346828968~9406473537"/>
        <!-- Play Games Services — the id lib/data/pgs_app_id.dart pins. -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/game_services_project_id"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
