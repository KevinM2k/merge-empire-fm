# Flutter
build/
.dart_tool/
.packages
.flutter-plugins*
pubspec.lock

# IDE
.idea/
*.iml
.vscode/

# macOS
.DS_Store

# Native build output
ios/Pods/
android/.gradle/
android/local.properties

# Secrets
*.keystore
*.jks
google-services.json
GoogleService-Info.plist

# Flutter tooling
.idea/

# Coverage output
coverage/

# Python bytecode from tool/*.py
__pycache__/

# `/graphify`'s build output — a knowledge graph of this repo, rebuilt on
# demand. The SKILL lives in `.claude/skills/graphify`; its output does not.
graphify-out/

# design-system (the React kit synced to claude.ai/design)
design-system/node_modules/
design-system/dist/
design-system/storybook-static/
design-system/.shots/

# design-sync: derived, regenerated, or transient. The durable half of
# .design-sync/ — config, NOTES, conventions — IS committed.
.design-sync/sb-reference/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.ds-sync/
ds-bundle/
