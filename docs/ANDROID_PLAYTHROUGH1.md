# Android playthrough 1

The rows a player wrote down playing the build on an Android phone, newest
session. A row is ticked when it is fixed **and** pinned by a test; the note in
brackets after it is what was actually wrong, because that is the part worth
keeping.

## Where this queue stands

**64 done, 15 open**, and the fifteen are all of one of six kinds — none of
which is "not got to yet":

- **Four are BLOCKED on the spec repo.** The headband, the laurel, the coat and
  suit shapes, and the facial-hair positions are all `manager_art.g.dart`,
  generated by `tool/gen_manager_art.mjs` out of
  `../merge-empire-fc/src/data/managerAvatar.js`. That repo is not cloned in a
  cloud container and nothing ending `.g.dart` is edited by hand, so redrawing
  any of them there would be inventing art and calling it a port — and the next
  generator run would undo it. **On the author's own machine these are simply
  open**, not blocked.
- **Four want a SCREENSHOT.** The momentum arrow (the code already does both
  halves of what the row asks for), the scorer's name in the replay (one real
  divergence found and fixed; what is left cannot be reproduced by reading), the
  grey buttons on the Play tab (the fault that description names has been fixed
  everywhere it can be found and `architecture_test` now fails the build on it),
  and the light-mode red and green.
- **Two are DECISIONS rather than edits**, and both say so in the row: the
  walker's scale, and how much room the daily-reward tiles should take.
- **Two want a DEVICE re-test rather than more reading.** The walk that pauses
  about once a second, and the Players page that still will not bounce. The
  first has had the likeliest cause removed underneath it since it was written —
  the whole tab body was repainting on every beat of the coach's pulse ring, and
  a pulse is about a second — so it is worth another look before anything else
  is measured.
- **One is a native SDK**: Meta's Aggregated Event Measurement on iOS.
- **Two are still open as WORK**: the goalkeeper practice wanting a keeper's-eye
  view, and the ball that sometimes moves with nobody near it.

---

Dark Mode
---------
Home
[] The manager is too low, he should be about 12px above the customise button at all times.  **THE 12px IS ALREADY THERE, so this needs a decision rather than an edit.** Measured through the real screen at four phone widths: his boots sit 13.3px above the pill's top edge at 320, 360, 393 and 412, `_measure` in `home_screen.dart` re-reads the pill after every build so the footer growing a cup button or an event strip moves him with it, and `home_screen_test` already pins `feet == pill.top - 12`. The spec agrees the anchor is right — `--ps-walker-bottom: calc(var(--ps-footer-h) + 58px)`, where 58 = 12+23+10+3+10.
    What IS missing is the SIZE. `LeagueScreen._syncWalkerScale` sizes him continuously from the band of grass between the match card's bottom and his feet — `clamp(0.95, 1.35, band * 0.8 / 140)`, the quest block deliberately measured OUT so opening it does not resize him — and 1.35 is the ceiling because it is the size the rig was tuned at. The port has one hardcoded `walkerScale = 1.22` at every screen size, so a tall phone draws him ~10% shorter than the spec does and, his boots being pinned, his head sits ~18px lower; a cramped phone has no floor to give and he crowds the card instead.
    **The decision:** 1.22 was chosen deliberately (see the note on `walkerScale`) because at 1.35 he was competing with the stadium now that the horizon is down. Porting the rule overrides that art call. Either port it and keep 1.22 as the CEILING rather than the constant, or accept the size. Wants your eyes on him at 1.35 before anything moves.
[x] The energy popup has 'Not enough Gems' for energy refill, it should not say that and it should show the blue gem button, it should do the same CTA as the energy fill in the shop - it does NOT need a link to 'Shop'  (the Shop's own rule — `purchase_flow.dart`: "Not enough gems is never said" — and this sheet was the last place in the game still breaking it. The gem button runs `offerToBuy` now, so a wallet that cannot cover it ends at the gem packs instead of at a greyed-out button, which is also what the Shop link was there for)
[x] The gem popup does not need a link to the 'Shop'  (both currency sheets — the sheet IS the shelf, so the button offered to take you somewhere to look at what you were looking at)
[x] The popups should only take as much room as they need  (**the frame had it exactly backwards, and the spec says so in its own words.** `.ps-sheet .ps-panel` is `height: auto; max-height: min(85vh, 780px)`, over the comment "tall sections (table, fixtures) all clamp to the same height, short ones (training) don't leave a void below their content". The port made the fraction a `FractionallySizedBox` — a HEIGHT — and then gave twenty-three call sites their own guessed number, so every sheet was that fraction of the phone whatever was in it. It is a ceiling now, with the spec's absolute 780 on top of it, which nothing had. A body still has to be able to say how tall it is, so the short sheets got `shrinkWrap` / `MainAxisSize.min` / `Flexible`: quests, energy, auto-sell tier, the age gate, connect account, daily reward, sell, the asset ladder, training, the formation and tactic pickers and both benches. **The long ones were deliberately left filling** — the league table, fixtures, the pyramid editor, the customiser, the slot picker — because they exceed the cap anyway, so the drawn result is identical and `shrinkWrap` on a list that long is a full layout pass for nothing. Three needed nothing — the income ledger, the currency sheet and the match statboard were already `MainAxisSize.min` or a `SingleChildScrollView`, both of which hug once the frame stops dictating a height. The empty benches were the worst of it: 72% of the screen holding one sentence, because `Center` fills)
[x] season quests popup is too big, too much room at the bottom  (the same fix, and this is the sheet it was found on — a `ListView` of three tiles filling 0.8 of the phone. Pinned twice: once on the frame's own contract, once on this sheet being the list plus its chrome and nothing else)
[x] season quests have 52000 as rewards, should be 52.0k or just 52k if its 0 after the 52. (`formatCoins(trim: true)` — a `#,##0.#` pattern, so German keeps its comma and loses the zero too)
[] goalkeeper practice is good, but its meant to look like the ball is coming towards you, so i wonder if it could look like  aview from the goalkeeper out, the ball doesnt have to be kicked towards them and you dont need to see the goalier, maybe just see the posts, and the pitch in front of us, and then we just do exactly whawt we do now.
[x] throughball, we have 5 of them, we have loads of room, why dont we have each of them under each other, the top one goes first, then the second one and so on until all done, then you can see them all from the start (they dont start until the last one is finisheD)  (five lanes, stacked, all with their green from the first frame; only the live one carries a marker, and a played one keeps its marker frozen where it stopped in the colour of the answer — the hit or the miss as evidence rather than as a word. **It makes the difficulty curve visible for the first time**: the zone shrinks 100% → 85% → 70% → 55% → 40% by round, so the last lane's sliver is on screen while the first is still being aimed at, and the player can see what they are aiming into before they get there. The five zone draws move to `initState` in the same order they happened one-per-round, so a session's zones are the sequence they always were)
[x] cant see the money on the training sessions, put them all in the ooin yellow. and make the little arrows white in dark mode  (both wore `drillTint` — the kit's accent walked up to 252° round the wheel, so seven rows quoted their money in seven colours. `coinFigureInk` for the money, which carries its own light-theme shade; the arrow keeps the tint in light mode, where it is what tells the rows apart)
[x] on pitch invaders, the whole doesnt really look like a hole, but everything else looks good, make the whole better.  (**`BoxShape.circle` was the whole of it.** The spec's `.pi-mouth` is a box 76% wide and 40% tall carrying `border-radius: 50%`, and in CSS that is an ELLIPSE filling the box — wide and flat, which is what a hole in the ground looks like from a low angle. Flutter's `BoxShape.circle` is not that: it draws a disc of the box's SHORTEST side, centred. So the mouth was a small round coin in the middle of a square of grass — and this also explains why the previous pass did not fix it: that one widened the box from the spec's 12% inset to 5% and it changed nothing at all, because a circle does not care how wide its box is. The inset is back at the spec's 12% now that width means something, and the ellipse is still nearly twice the area the disc covered. **The shading was the other half and none of it was ported**: the mouth's `inset 0 5px 9px rgba(0,0,0,0.9)` is the far wall of the cavity in shadow — the single thing that turns a dark shape lying ON the grass into a hole you can see over the edge of — with `0 -2px 7px` cast up onto the turf above the rim as its lip, plus the tile's own dish (`inset 0 2px 6px` white at the top, `inset 0 -8px 12px` black at the foot) and the same foot on the occluder band. Flutter has no inset shadow, so each is the gradient it would have drawn, clipped to the same oval)

Players
[x] When clicking on a player, the cancel button looks almost greyed out, and has the weird top 3d on it  (**both halves were one call site fighting the theme**, in `sell_sheet.dart`. Greyed out: it asked `styleFrom` for `foregroundColor: kit.textMuted`, the exact colour `mouldedButtonStyle` uses for `deadInk`, over `side: kit.border`, its disabled border — so the only live way out of an irreversible sale wore a disabled button's ink on a disabled button's outline. The top 3D: `side` is drawn by the button's own Material on the FULL 48pt rect while the moulded face sits in a 40pt box 4pt inside it, so it added a second outline 4pt ABOVE the first. Both overrides deleted; the theme's moulded outline is what a cancel is supposed to look like. The Sell beside it had the third form of the same fault — `backgroundColor` fills the Material BEHIND the face over the full rect, burying the hard bottom edge, which is why it read flat)
[x] A loan player says is not yours to lend on... if our player is loaned out this should just a recall button or should say you cant sell a player who is loaned out, similar to a player we have on loan, it should be send back or cant sell a player who is not ours  (**both directions were one sentence, and it was the wrong one for either.** `sellBlocked` has always returned `on_loan` and `loaned_out` separately, so the engine knew — but `sellBlockedCopy` folded them onto `event.deadline.blocked_loan_card`, 'A borrowed player is not yours to lend on.', which is about LENDING, was written for the Deadline Day board, and is backwards for a player of ours who is away. The two sentences asked for here already ship, translated in all ten catalogues, **with no caller anywhere in `lib/`**: `squad.detail.cannot_sell_loan` is 'On loan — not yours to sell. {matches} games left.' and `squad.detail.cannot_sell_out_on_loan` is 'Out on loan at {team} — recall {name} before you can sell.' The JS prints the second on the squad detail sheet, whose own Sell button has since gone, so this sheet is the only sell path left and is where both belong. The recall itself already exists on the squad sheet — `_recall` — so the copy names a control the player can reach)

Squad
[x] the picture of the cards is bigger than the text and border, it means the image is kinda cut off at the top too  (**both halves are one line of CSS the port never had.** `.card` is a flex COLUMN — a 4px `.card-strip`, then `.card-artwrap` at `flex: 1` with `margin: 2px 3px 0`, its own 6px radius and a rarity tint, then `.card-footer` — and `.card-img` inside that well is `object-fit: contain`. The port drew the portrait as a `Positioned.fill` across the whole card at `fitWidth` anchored to the top, with the chips and the caption floating over it: scaled to the card's WIDTH, so wider than the well the spec gives it and taller than the room left over, and pinned to the very top, so the top of the drawing sat under the border and under the rating chip. A pass before this one had tried `contain` and backed it out because it left a band of nothing above his head — that was `contain` over the whole 3:4 CARD, where a square drawing loses a quarter of the height to slack. The WELL is the card less the strip and less the footer, which at bench size is 96 by 89: near enough square that the whole figure fits with three points to spare either side. The footer becomes a real band with the spec's hairline of rarity along its top, because there is no longer a portrait behind it to fade into)
[x] When clicking on a player we dont need the pull bar on the right of the trait roller, just the button is fine, but we should make the text on the top and bottom item in the roller either slightly transparent or more screwed, this should kinda like it spins around on a circular roller  (the lever goes — a divergence from the spec, which has a rod and a weighted ball, and the gold pill below is the only control a roll needs since it is the one that says what one costs. **The fade was in the spec all along and had never been ported**: `TraitRoulette.js` ends its face with a `linear-gradient(180deg, surface 0%, transparent 10%, transparent 90%, surface 100%)`, so the port's three rows were equally lit and read as a list with a stripe on it. Both halves are here — that gradient, and the part a DOM strip cannot do at all: the reel is a real cylinder now, `overAndUnderCenterOpacity` at 0.42 and `diameterRatio` down from 1.6 to 1.1, so the rows either side dim AND turn away. The reels also stop drawing a second `surface2` box with a second border inside the window's own; the window is the frame)
[x] when clicking a roller it should roll them properly, both of them, for maybe 3 seconds or so before it lands  (**the spec says 3 seconds almost exactly and the port was running under two.** `ANIM_MS = 5000` with the name reel stopping at 58% of it, which lands the answer at 2.9s and the level two seconds after — 900ms shipped first, 1900 replaced it as a guess, and neither is the JS. The other half was the CURVE: `Curves.easeOutCubic` is pow 3 and is 87.5% of the way home at the halfway mark, so seven eighths of the travel happened in the first second and the rest was a reel inching onto its stop. The spec's own `1 - Math.pow(1 - t, 2.5)` has no `Curves` member, so it is a four-line `Curve`. Laps come down from five to the spec's three, since the travel is now spread over five seconds rather than crammed into two)
[x] When clicking on swap, the players are listed in a list view, we should just list them in a 3 on a row - like we do for subs, ordered by post in teh position chosen (should put that filter on the top of the subs bench too and auto select the position someone is being swapped from unelss none, then it would go to all)  (three parts. The GRID is a divergence — the JS lists candidates as 40px thumbnails on rows of text and the port had followed it, so the one sheet in the game where a manager compares players showed them smaller than the bench two taps away did; it is `benchColumns` now, the bench's own count, with the eff-rating pill kept under each card because what a man is worth IN THAT SLOT is the only reason this sheet is not just the bench again. **The bench filter, though, is the spec's and was simply missing**: `_benchFilterDefs()` is built once in `SquadScreen.js` and hung on BOTH the picker and the bench sheet, and the port had put it on the picker only — so the one sheet that runs to twenty-odd cards was the one with no way to narrow it. One `PositionFilterBar`, both sheets. The auto-select already worked; what it lacked was the **unless none** — the JS defaults to the slot's line and stops there, so a squad with no reserve keeper opened the GK picker on an empty sheet and a chip you had to work out to un-press. It opens on ALL when the line offers nobody)

Club
[x] each asset blends too much in to the background, maybe we should make them a slightly different colour so they stand out, id like them to almost look like thely popout the screen  (**the fourth report of this, so the FACE changed rather than the edge again.** Every pass before went at the outside of a flat `surface` fill — a stronger border, then a shadow, then the art — and a flat fill is the thing that reads as page: on humbug and turf the card is literally one of the two band colours, and on a derived kit `surface` is two steps off `bg`. The app already has a vocabulary for sitting on top of the screen and it is not a border. It is the shop's `.look-tile`, which fixed this exact complaint on the look packs a commit ago: `surface2 → surface` raked down and right, a wash of the tile's own colour at the lit corner, and a shadow. Both shadows on the dark page now too — a soft cast for the height and a tight contact for the weight, where one blurred shadow read as a glow. The tier's metal is the wash and the border, so a gold Stadium and a bronze Academy are not the same grey card twice; faint, because seven saturated cards is a paint chart and the tier is already on the badge)
[x] the humbug design has far too many black and white stripes, makes it hard to see, we should make the stripes wider and the colour should be a bit more transparent so we can read things on it.  (**the port was painting the SHIRT's stripes behind the app, and `kit_palette.dart` says in its own comment why that cannot work**: "the app CHROME cannot use true black and white — white body text sits directly on it, which is why the theme pairs near-black with a mid slate — but a shirt carries no text, so the kit itself gets the real humbug". `_backgroundFor` reached for `kitPatterns[...].stripes` anyway — `#111111` against `#f0f0f0` — so every screen in the game stood on fourteen bands of true black and true white with copy over them. `HUMBUG_BACKGROUND` in `kitTheme.js` is `#0e0e0e` against `#3a3a3a`: the same fourteen bands, whispering. **And there was no light variant at all** — `HUMBUG_BACKGROUND_LIGHT` and `TURF_BACKGROUND_LIGHT` were never ported, so a light-mode humbug drew dark body text over true-black bands and a light-mode turf drew it over forest green. Turf's dark pair was slightly off too. The WIDTHS were already the spec's 28 and stay: once the bands stop shouting the count stops being something the eye fights, and widening as well would be treating the one symptom twice — say the word if you still want them wider, it is one number. Turf's second layer, a 45° hatch of white at 3%, is noted as an unported gap in the code)

General
[x] There is a new app icon called 'e08bdf35-5ae9-4a01-bb2f-115b5f44d89b.jpeg' in the Personal folder on desktop, please use this for the app icon and for the splash screen stuff.  (the master under `tool/gen_app_icons.py`, so it stays regenerable. The new art is FULL BLEED and the shield it replaces was not, which is three separate assumptions in that generator: the adaptive background can no longer be a corner sample of a flat field — it is the perimeter average now, so a mask showing more than the 72dp safe zone reveals a band that belongs to the picture; the splash logo can no longer be a cut-out, so it takes iOS's corner radius and reads as an icon rather than a stray square of scenery; and the launch background stays #0A1A0F because `LaunchScreen.storyboard` and `boot_splash.dart` both hardcode it and the game it hands over to is dark. The foreground still inlays at 72/108 — full-bleeding it to 108 hands a circle mask the middle two thirds and cuts the manager in half. The source is 512, so the App Store's 1024 is upscaled)
[x] End season page has everything on background, everything sholud be in cards, seperate tehm in to lgoical groups.  (**and the spec has had the groups the whole time — the port had ported the contents of all of them and none of the containers.** `screens.css` carries `.se-eyebrow`, `.se-hero` (the division, the place, the verdict and three `.se-stat` tiles in one box, with a 3px hairline of the outcome's colour across its top edge), `.se-line` for the one-line facts, `.se-card` for the blocks that hold rows, and a PINNED `.se-cta` at the foot — the spec's own note: "the way out of this screen is never more than a thumb away, however long the summary above it runs". Every one of those was a bare `Text` in a `Column` on `kit.bg`, at the same distance from the page and from each other, which is why nothing said what belonged with what. **`--se-tone` is the other half**: the outcome sets one colour for the page — gold for a title, the accent for a promotion, red for the drop, and gold on the NUMBER ALONE for mid-table since survival has no colour of its own — so four outcomes are four differently-coloured pages rather than one page with a different sentence in it. The payout moves into the pinned foot with the Continue button, because it is what the button collects. **The material is deliberately NOT the spec's glass**: `.se-*` is takeover glass because the JS's season end sits on the sky and this page is flat `kit.bg`, and the club screen settled that question two items ago — these are the same lifted surface the asset cards now wear. Still unported and noted rather than forgotten: the quest ROWS that fold open inside the quests card, and the pinned block's watch-an-ad-to-double-the-payout offer)
[x] the rating in the next match card only shows one decimal, so if a team is 91 vs 81, all we see is 8 vs 9  (**the ATK/DEF figures, and the port had the spec's flex backwards.** `.nm-stat-val` is `flex: 0 0 auto; min-width: 19px` against a `.nm-stat-bar` of `flex: 1` — the figure never shrinks, the bar does. This gave the figure a 2/7 proportional SHARE of its side, and a share is not a leftover: the box measured 9.9pt at a 390 viewport and 4.9pt at 320, narrower than one digit, so every two-figure stat was clipped to its first digit at every width the game has ever run at. The 19 in the code was dead. Fixing it also needed the spec's `@media (max-width: 379px)` rule, never ported — under 380 the bars come OFF, because their width is derived from the card's and at 375 every one bottoms out on its 12pt floor, four identical stubs comparing four different numbers. The card's real inset is the viewport less 42, and with that the spec's own arithmetic reproduces exactly: 15pt bars at 390, 11.25 at 375)
[x] We should block horizontal play on phones, there isn't enough room, on tablets/folds its ok, but on normal phones it is not.  (`ui/shell/orientation_lock.dart` — 600dp of SHORTEST side, which is Android's own tablet threshold and does not move when the device is turned. A fold opening is re-measured; the keyboard is not)
[x] I drew 1-1 in a cup game, it should have went to pens, but instead it didnt, it came up defeat and siad they won 1-2  (the pens DID happen — the engine folds the winning kick into the scoreline so `won` and the score agree, and a parity fixture reads those fields, so it printed 1-2 with the shootout reported below the league table and the scorers, i.e. below the fold. The divergence is on the screen: the card prints the ninety minutes and the pens sit directly under it)
[x] The income popupu when you tap coins at top doesnt need to be a coach colin popup, it should just be one fo those bottom popups instead, want to reserve coach colin popups for when its him telling us something, this one is like looking at the books  (a `fitContent` sheet, so a short ledger is a short sheet)
[x] No prestige badge top left next to the trophy, the multiplier looksl ike it works, but it needs to show prestige count in the top left as well.  (`.hud-prestige` was never ported — a star, a `×` and the figure, on the resource pill's own trough, and nothing at all at level zero)
[x] Prestige should add 0.1 for each prestige, so i've prestiged twice, so my multiplier should be 1.2x, right now its 10% increment, which means its totally broken if someone has 50 prestige.  (the JS compounds — `Math.pow(1.1, level)` in `LeagueScreen.js` — which is 117x at fifty. A tenth ADDED: 1.2 at two, 6 at fifty. The migration recomputes an old save through the same sum, and `game_state_test`'s whole-save parity now normalises that one field on both sides rather than going unchecked)
[x] Tarnsfer offers seem to come in to fast, sometimes dont show (just the bar at the bottom) and im not a fan of the yellow thing at the bottom, i dont mind where it is but we need to make more obvoius that you click on it to open, maybe 3d maybe gradient etc (the BAR is done — gradient face, hard edge under it, chevron, drops on a press. Pacing and the sometimes-doesn't-show half are still open and are a different bug)
[x] On the bid popup, 'Accept Offer' is too much text, we can use 'Accept'  (swapped to `common.accept`; every non-English catalogue already said just "Accept")
[] Daily reward popup has so much more room, use it  (partly: the tiles are 118 rather than 92 to hold the reward chips. **The room under the strip has now GONE rather than been used** — this sheet was one of the ones fixed by "popups should only take as much room as they need" above, so it hugs the strip instead of standing at 0.85 of the phone with a void under it. Two readings of one sentence and they pull opposite ways: if the ask was the void, it is closed; if it was that the DAY TILES should be bigger, say so and they can grow into the cap. Left open on that)
[x] we use an emoji for coins on the dailyi reward, it should be the coin and it should be using the coin yellow.  (💰 was the one money glyph in the game not drawn in the app's own icon set)
[x] Each one needs to highlight eaceh of the rewards in seperate boxes inside each day, we have a lot of room.  (a pill each, in the wallet's own colour — the HUD's coding: gold money, violet energy, cyan gems. The joined-with-middots line survives as the tile's `Semantics` label, since chips say nothing out loud)
[x] global leaderboard, it doesn't show my rank just has - (the fetch has carried `playerRank` all along — the card now reads it. The dash is kept for a board still loading and a board that has never seen you)
[x] we have a place for 'trophies and record' im not even sure what they are, lets drop them. (both tiles gone — one was the trophy room's reading, the other the season report's)
[] I get the following when trying to do meta ads on ios: Your app can't use Aggregated Event Measurement because conversion data is missing or partial. Usually, the easiest solution is to set up the Facebook SDK for iOS.

Customise Manager Popup
[x] the body needs to be slightly more zoomed out  (the camera steps back rather than the figure shrinking — he was 170 of the stage's 190, so the ground he is being dressed for was a strip either side of his shoulders. The half that is easy to get wrong is the other one: `Align` centres the BOX, so a figure scaled down under a fixed alignment lifts off the grass by half of what came off his height. `_standAlignment` is derived from the scale and the sole line, so neither can go stale, and the test measures his soles against the drawn grass rather than his box against the stage)
[] the manager walking seems to pause every second or so for a brief second  (**RE-TEST THIS: the likeliest cause has just been removed from underneath it.** The stride itself was audited from here and is clean — one `Ticker` in `WalkClock` counting half-strides with a 0.05s clamp per frame, and nothing in it stops, restarts or re-phases on a cycle; `_sync` touches the clock only when the MOOD's duration changes, which is once a season. So a pause has to be a dropped FRAME rather than the walk, and the row below found exactly one: the whole tab body was repainting on every beat of the coach's pulse ring, and a pulse is about a second. Worth another look on device before anything here is measured again)
[] the suit/coat dont look like suits or coats, they need more work.  (**BLOCKED, and specifically on the GEOMETRY half.** An outfit is two things: a palette, which is `outfitPalettes` in `walker_figure.dart` and belongs to this repo, and geometry — a coat's skirt, a suit's lapels — which is `managerOutfits` in `manager_art.g.dart`, generated by `tool/gen_manager_art.mjs` out of `../merge-empire-fc/src/data/managerAvatar.js`. That repo is not cloned in a cloud container and nothing ending `.g.dart` is edited by hand, so the shapes cannot move from here. What DID change is the row below: the garment was being painted straight over the near arm, which is most of why a coat read as a charcoal slab with a head on it)
[x] the front arm should be in front of the coat/suit ont behind it  (**the spec says so in the slot's own comment** — the outfit overlay is "drawn over the torso, hips and near thigh, under the head and the near arm, so the arm still swings in front of the coat" — and the port had it over the WHOLE rig. The near arm is the last thing the painter draws, so a coat or a suit buried it: a slab with one arm swinging behind him and none in front. It is the same second pass a hand at the face already gets, and only for an outfit that HAS geometry — the plain kit is a palette swap and pays for nothing)
[x] the toast that pops up is ok, but it needs to be more visible righw its just a clear box with a red border, make it stand out more  (**"a clear box" was literal.** The ground was `kit.surface` — the value the page behind it is built from — so the box had no edge of its own and what was on screen was a red outline floating over the scene. It takes the chips' own `semanticPlate` now, tinted by the tone and composited onto the surface so it is opaque in both themes, with a drop shadow under it. **And the tone runs all the way through**: the ink was the kit's accent whatever had happened, so a refusal was a GREEN sentence inside a red outline)
[x] when clicking on an item with an advert, i love the way it comes up at the top however the user doenst need to click the x for it to go away, they can just tap on another 'celebration' or item for it to change to that one. The buttons however do need a little more height to match others  (two halves. A locked item is laid over the save for the PREVIEW only, and only `_offerLockedItem` ever cleared that — so tapping another locked chip swapped it, and tapping one you already own left the locked item on the rig under a bar selling it, with no way out but the ✕. `_set` and a played celebration both answer the offer now. The buttons were `StoreButton(small: true)` — the ROW variant, 11pt type in a 9pt radius, for a list line — and these two are the only controls on the stage; same call the shop's pack pill got, for the same reason)
[] be careful of teh facial hairs, each needs measuring against th eface, some of them dont stay on teh face or are slightly wrong position  (**MEASURED, and it is the ART rather than the port — which was worth proving, because there was a good reason to think otherwise.** The port redrew the JS's plain circular head as a skull with a nose and a jaw narrowing to a chin, and the generated beards were drawn against the circle; if the two disagreed, every beard would sit wrong at once. They do not: `_HeadPainter` keeps the circle exactly where the art puts it, (62, 48.5) r12.5, and says so in its own comment — "moving it by a unit puts a hat on his ear". Each beard's paths were then parsed and measured against the drawn silhouette. `stubble`, `beard` and `goatee` finish within half a unit of the chin and inside the jaw. `muttonchops` is an arc of the skull's OWN r12.5 from (51.3, 42) to (57.1, 60), and the port's jaw curve passes through (57.05, 59.65) — 0.35 of a unit away. The two that hang are `full` and `braided`, both reaching y 73, eleven units below the chin and over the collar; whether that is a long beard or a wrong one is a look at the drawing, not a fault in how it is drawn. `managerBeards` is generated from the spec repo, so the shapes cannot be changed from here in any case)
[] headband doesnt look right, please check it out.  (**BLOCKED on the spec repo.** `managerHats['headband']` is a single path in `manager_art.g.dart` — a band across the brow from (48.6, 44.5) round to (75, 44) — generated by `tool/gen_manager_art.mjs` from `../merge-empire-fc/src/data/managerAvatar.js`. Nothing about where it lands is the port's: it is drawn in the same space as every other head layer, moves with the group's set-back and lift, and is clipped only by a hat's own brow line. So the SHAPE is the change, and its source is not cloned in a cloud container)
[x] even ones that you can only go via a cup unlock or something it should show you what it looks like with the same kinda popup we have on the advert ones but this time it should say 'Unlocks at xxxx'  (**and the copy for it already shipped.** A lock a video cannot open — a Fan Zone tier, a cup — stopped at a toast, on the reasoning that a bar with two dead buttons is worse than a sentence. Which is true and answers the wrong question: the SHOWING has nothing to do with whether there is anything to sell, and what the player got was a sentence about a hat with the hat still not on him. Every lock tries the item on now and the bar drops its BUTTONS rather than the whole bar. No new key and there could not be one — `lockedReason` has always returned `customise.locked.fanzone`, "Unlocks at Fan Zone Tier {tier}", and `customise.locked.cup`, "Win the {cup} to unlock", both translated in all ten catalogues and neither ever printed anywhere but a toast that could not be acted on)
[] Laurel doesnt look right, have another go  (**BLOCKED on the spec repo, same as the headband.** `managerHats['laurel']` is a stroked arc with a row of ellipses hung off it, all in `manager_art.g.dart` and all generated from `managerAvatar.js`. Redrawing it here would be inventing art and calling it a port, and the next `gen_manager_art.mjs` run would undo it)
[x] fist pump a little laggy on android and goes a little too close to the face  (**the FACE half is done and it was measured rather than eyeballed.** The skull as DRAWN is a circle at (59, 41.5) r12.5 — the art's (62, 48.5) less the head group's set-back and lift — and at the old peak of (-84, -90) the hand was **0.96 art units** off it, which at this scale is touching. The lever turns out to be the ELBOW rather than the shoulder: `armNear` cannot push the elbow further forward than -90, which is 18 units dead ahead, so opening the elbow is the only thing that moves the fist away from the face — and it moves it FORWARD and UP. The peak is higher than it was, (78.3, 41.9) against (76.1, 43.4), with 3.09 units of daylight, and the pump is bigger: 16° of shoulder against 10°. **And the laggy half is very likely the pulse ring** — see two rows down, where the whole tab body turned out to be repainting on every beat of it. Worth re-checking on device rather than chasing the pose)
[x] arms folded need to try again, doesnt really look like arms folded  (**two faults, and the second is about paint ORDER.** The first is that the forearms were nowhere near each other — near hand at (67.5, 79.4), far at (67.2, 68.9), ten and a half units apart, which is one arm across the belly and another across the chest. They are 5.4 apart now, both across the mid-chest, the near forearm lying on top. The second is why it read as ONE arm however the angles were set: `_WalkerPainter` draws far arm, then body, then near arm, so the far forearm is BEHIND the torso — and the torso runs x 47.9 to 69.9, which is exactly the band a folded forearm crosses. It was drawn and then painted over, every frame. The far hand finishes at (72.2, 76.5) now, past the shirt's front edge, and the two of them together are what makes it read as folded)
[x] wave - the arm goes behind the head for some reason  (**it is the ELBOW, not the hand.** The wave was left out of `gestureHandsOverHead` on the reading that its hand clears the head — it does, at (68, 26), ten units above the skull — but the shoulder is at x56 and the skull's centre at x62, so an arm raised to -150° folds through the middle of his face at (65, 46). Any raised near arm crosses the skull in this rig, and in profile the near arm is the one closest to the camera, so in front is where it belongs)
[x] kiss the badge should touch chest then kiss the hand  (**a deliberate divergence from `psvGestBadgeArm`, because the JS's own angles touch neither.** Solved against the rig instead of eyeballed: `arm -30, fore -118` puts the hand at (75.9, 60.1) — held out in FRONT of his chest, six units clear of the shirt — and `arm -52, fore -146` at (63.8, 53.5), the middle of his own face rather than his mouth, and BEHIND it, because the gesture was not in `gestureHandsOverHead` either. So it went out, in, out: a man patting the air twice with a pause at his nose. It touches the badge at (68.8, 68.5), holds, then takes the hand to the mouth at (72.3, 51.0) — the blow-kiss's own hold, which the spec's stylesheet annotates as "on the mouth" — and does not come back to the chest, because the kiss is the end of the gesture)
[x] lots of animations a little laggy on android - need work - please check that there aren't multiple things redrawing multiple times on pages that would cause this type of lag. we had this for the manager customise rig initially  (**profiled on the Fold 7 in profile mode, and it was exactly that.** Home idled at 22ms a frame — 99% over budget before a finger touched it. Half the UI thread was `_StandPainter`: the crowd bounce repainted every fan every frame, and even still, the five tiled stands sat under `_Scroller`'s per-frame translate with no boundary, so `shouldRepaint` never got a say. The crowd is still at rest now and bounces only for a surge; every static band goes through `_StillStrip`, a `SnapshotWidget`, because Impeller has no raster cache and a boundary alone still re-rasterised them. Home is 11ms, 3% over budget)
[x] on cigar animation there should be little bits of smoke coming out of it  (**IT ALREADY HAD SMOKE, and this is the shipped-asset-with-no-caller tell in a new place.** `managerFaces['cigar']` ships a `<g class="mgr-smoke">` of three `.mgr-smoke-puff` circles — r 1.5, 1.1 and 1.8 — and all three sit at the SAME point, (83.6, 51.8), because in the JS that class is a CSS animation and the file only states where each puff starts. The port draws the file, so the three were one grey disc on the lit end that never moved. They rise, drift forward and fade on their own clock now, staggered a third of a cycle apart, keeping the file's own three radii and its ember position; a stopped clock (reduced motion) leaves them at three different heights rather than back on top of each other. The static group is cut at the same boundary that recolours the art, or the disc would draw under the animation. The breath's own `_drop` comes out of the comfort painter to be shared rather than copied)
[x] the merge empire football manager text on the hoardings in the background need to move down slightly to be vertically centered.  (**two faults, and only the second had been noticed.** The centring is the one asked for: the LINE BOX was being centred and the ink is not the line box — every letter on a board is a capital, so the ink runs from the baseline up to the cap line and the room a box reserves for descenders is empty, which hangs the lettering high. It centres the cap band now. **The first fault is that it was WRAPPING**, which nothing had measured: the mark is 29 characters and the pale half of a 240 panel is 120 wide, and at 5.5 that is not enough in every face the platform might resolve — the test binding's own fallback wants 179.8 and breaks it over TWO six-unit lines inside a board 13 tall. Whether it wrapped at all depended on which font came back. It measures what the mark wants unconstrained and scales the type to fit on one line, and the cap band is then computed from the size it was actually SET at rather than from 5.5 — or a shrunk mark would get most of the offset put straight back)
[x] Occassional lag when playing on android and going through screens/popups  (**the whole tab body repainted on every beat of the coach's pulse ring** — same Stack, no `RepaintBoundary`, so on Players every card and every dashed slot re-extracted its outline each frame. The body and the coach layer are their own layers now; each grid card has one too, so a merge ring pulses one cell rather than the grid; the dashed outline is cached per size; and the income bar is a painter off the clock in a 3pt layer rather than a `BarFill` relayout of every card. Squad, Club, Shop and Settings idle under 1ms of build. Scroll is `BouncingScrollPhysics` via `MaterialApp.scrollBehavior`, which is the native-feel ask for those four pages — but see the open Players item below. **And `floatCoinTick` was never ported**: `Grid.js` floats the payout off a card every bar cycle, so idle money is now seen leaving the players who earn it — `coin_tick_float.dart`)
[] Players page still does not bounce at the top and bottom, on device, after the app-wide `scrollBehavior` change. Odd, because `merge_grid.dart` is the ONE list that already names `BouncingScrollPhysics(parent: RangeMaintainingScrollPhysics())` on its `SingleChildScrollView` explicitly. Reproduced by feel only; not yet profiled. Suspects: the drag-to-edge `_autoScroll` / `_edgeScroll` timer fighting the overscroll, or the shell's horizontal-swipe `GestureDetector` winning the arena at the ends. Check on device before touching the physics line.

Game Popup
[x] the user should always be the green team, the opponent should always be red, atm the scoring team is always the green regardless of home or away, thats not right  (`cutaway_game.dart` gave the ATTACKERS green and the DEFENDERS red, so an opponent's goal replayed with them in green attacking our reds. The kit follows the club now, which is what the labels — our names, their numbers — already did and what the idle pitch has always done)
[] the player name needs to match in the commentary who scored it and when viewing replay it needs to be identical, same play, same player, same goal  (**PART DONE, and the rest wants a screenshot.** One real divergence found and fixed: the feed's commentary read `event.scorer` — a name SNAPSHOT taken when the events were generated — while everything the 2D pitch draws resolves the card LIVE through `cardDisplayName`: the shooter's own dot in `cutaway_game.dart`, the replay's badge and the full-time scorers card. Two readings of one question, agreeing only for a card nothing has happened to. `feedOf` takes a `nameOf` resolver now and the screen hands it the save, with the snapshot kept as the fallback for a card the save no longer has — the one case a live lookup cannot answer. **What was checked and is already right**: the replay rebuilds its clip from the same event, the same seed (`result.seed + minute`) and the same `ourSideLeft`, and `CutawayGame`'s own `Random(seed)` means the arrangement is reproduced rather than re-rolled; the scorer's name is forced onto the shooter's dot in `_shoot`, and every other dot's name is held back from the pool so no second figure wears it. Two things could still make a replay differ and neither is reachable from here without a case: a SUB between the goal and the replay changes `lineupNames`, so the other dots are named differently; and the badge under the replay prints the SURNAME only, because his full name will not fit under a 72px circle, which is a deliberate constraint rather than a different man. Wants a screenshot of the two side by side)
[x] sounds are not matching up with whats going on, seems to be like miss noises happening but with no action, if no action... no noise...  (**exactly right, and the count is the tell: there are about thirteen chances in a match and the FEED prints three or four of them.** A chance earns a line only if it was big AND on target AND far enough from the last one that was SHOWN — but the sound was hung on the EVENT, so all thirteen played a kick and every on-target one played the crowd's groan on top of it. Nine or ten noises a match with nothing on screen to belong to. `feedChanceMinutes` answers by running the feed rather than by re-stating its filter, which matters here: the gap is measured from the last chance that reached the feed, not the last that happened, so a second copy of that arithmetic would drift the first time either half moved. It also keeps a chance the 2D pitch retold audible whatever its own numbers say, because a clip is on screen by definition)
[x] I feel as if the commentary is to quiet, especially when a game starts and nothing happens for 15/20m.. it should start always with something as if its a commentary team something like 'excitement around the stadium is high', or 'the fans expect some fireworks' etc etc.. more life, more action.  (**the 15/20 is exact.** `commentaryPools` puts the `open` bucket at [1, 1] and the next one, `firstA`, anywhere in 15..30 — so a match says one thing at kick-off and then nothing for between fourteen and twenty-nine minutes. **Chances cannot fill it**, and the arithmetic is worth writing down because relaxing their filter is the obvious first idea: there are about thirteen over eighty-eight minutes, so fewer than two land before minute fifteen and half of those are off target — letting every one through buys about one line. **And the pool table is not this repo's to move**: it is the JS's, and `match_orchestration_reference.json` pins the events it produces field for field, 363 commentary lines, which cannot be regenerated from a cloud container. So the divergence goes on the SCREEN, which is where this port puts them. The `open` pool has THREE lines and a match printed one; all three are said now, spread across the window the engine leaves empty, with the engine's own seeded pick still leading — what a match OPENS with has not changed, what follows it is two lines of atmosphere instead of silence. The filler is merged stably rather than appended, so a goal sharing its minute still reads first. **No new copy, and there could not be one**: 'The crowd are already loud.' is `commentary.flow.open.2` and has shipped in ten catalogues all along)
[] the arrow should be pointing towards me if they are on top and pointing towards them if im on top and the location of the arrow should show the dominance, i.e. closer to them  (pointing at them) if im dominating and vice versa **READ THE CODE AND IT ALREADY DOES THIS**, both halves: `momentum_arrow.dart` points at the goal under pressure and slides `travel * strength` toward it, and `momentum_bias` is chance attribution rather than possession. So either the reading is right and looked wrong, or `attackingRight` is being handed the wrong venue somewhere — wants a screenshot of a spell where it looked backwards before anything moves.
[] the ball still sometimes moving with no player near them.
[x] the players scored is cool, however i think the time they scored the goal should go on the left of the name so names are always in same place  (**the fix is on the FULL-TIME REPORT, not this popup** — `_ScorersCard` in `match_summary.dart` was the only `name minute'` in the app. This popup was already right: the feed's goal card leads with the minute in its head row and the scorer row under it carries the name and the career tally, no minute. A 28px column of tabular figures, so the list has a left edge to read down)

Light mode specific
[x] THe border around the tabs in settings are missing the top radius on the selected button so it overwrites the white borders a little  (the clip was on the box, which clips to the OUTSIDE of its border; it is a `ClipRRect` one radius in now)
[x] it has 'Send Feedback' coming soon in account, get rid fo this option (and the JS hides its own button for the same reason, so this was a divergence rather than a missing feature — see `feedback_service.dart`)
[x] Difficulty, we need somethign to show that its unlocked and they need to prestige to unlock  (the padlock, the dead segment and the answering tap were all already there — what was missing is that **nothing on the row ever said the word LOCKED.** The note printed `prestige.body_pro_hint`, "Or prestige into 🔥 Pro Mode — fatigue, squad rotation and live subs make it a real test", on the reasoning that saying the prestige card's own sentence makes the lock legible. It does not: that line describes what Pro IS, its leading "Or" belongs to the offer it was cut out of, and a player reading it under a greyed control learns what they are missing and nothing about how to get it. The CONDITION already ships, exactly, as `ach.desc.prestige_level_1` — "Prestige for the first time." — so the note is a padlock and that sentence, and the tap adds the description after it. No new key and there could not be one; the catalogues are generated. The padlock also grew from 12 to 14 and the locked CHOICE now steps back on its own rather than looking like an ordinary unselected segment — the group's existing dim is for a segment nothing can change, and Casual beside it is perfectly live)
[x] the coins on the home page are a horrible bronze colour, i prefer the yellow and i think itll still work here. (right — the bronze is for gold on PAPER and this card is glass over the pitch. Gold on glass, with the dark backing the HUD glyphs already use in daylight)
[x] we have +4 for home .. regardless if thats home or away, its a plus so it should be green only minus should be red on both sides (the sign decides the colour now, not the side; the `+` is printed off the number rather than hardcoded)
[x] we shoudl be asble to tap the +4 to get a small popup that tells us what it means, it should go away on its own. ALREADY DOES — a `Tooltip` on `triggerMode: tap`, which dismisses itself. Now pinned by a test so it cannot quietly go back to a long press
[] the red and green, should match the same red and green used in dark mode, looks better in dark mode. **LEFT OPEN, and it needs a decision rather than an edit.** This has now been asked four times and `match_stat_rows.dart` carries the arithmetic: `#4ADE80` is 1.9:1 as ink on the light theme's own surfaces, so the dark pair cannot be printed on a white page. The light pair is already the brightest member of each hue that clears 3:1, and `glass.dart` measures the rest against `paneLuminance`. The honest options are a dark PLATE under the figures in light mode — which `semanticPlate` already does for chips — or accepting the shade. Wants a screenshot of the exact spot before anything moves.
[x] the locked spaces on players tab are bright white, they should be subtle so you can see they are there but not opaque. (a wash of the surface rather than a second grey — the backdrop behind the grid is turf or humbug) 
[x] The auto sell tier needs a background, its transparent right now and its hard to see esepcial on humbug and turf backgrounds (both grid pills — the roster count's red at capacity had the same 25% wash. The tint composites onto the plate now instead of onto the page: `pillGround`)
[x] the dd player and mege buttons when can te clicked on too light, they should look disabled  (**the Deadline Day palette was mostly not the spec's.** `_ddKindStyleRaw` carries FOUR fields per kind — ink, tint, face, edge — and the port's `KindStyle` had only ink and tint, so the accept button reached for `styleFrom(backgroundColor: style.ink)` and got neither: `backgroundColor` colours the layer UNDER the moulded face, so every accept button on the board came out the theme's accent green whatever deal it belonged to, and the fill it did apply buried the button's hard bottom edge. Three of the four inks were also the wrong hue — loan was Material blue against the spec's purple, a loan out amber against teal, a plain signing purple against light blue — the MARQUEE branch was missing entirely, so the one listing meant to arrive in gold wore its kind's colour, and the ink never went through `readableInk`, which is what stops a `#7FD4FF` terms line being unreadable on a pale card. All four kinds now carry a face and an edge and go through `mouldedButtonStyle`. **Still open:** whether "too light" also means the DISABLED state wants more than `surface2` — that half wants a screenshot of a blocked listing)
[x] in deadline day, we should be showing the coach colin picture at the top, its an emoji atm (and the same emoji was on the cup view and the event banner, so all three now use `CoachFace` — the one crop of him the dock and his own card already draw)
[] the buttons in play screen are grey with a weird 3d effect.  (**WANTS A SCREENSHOT — the fault it names has been fixed everywhere it could be found, and this row can no longer be reproduced by reading.** "Grey with a weird 3D effect" is the exact signature of `styleFrom(side:)` on a moulded button: `side` is drawn by the button's own Material on the FULL rect while the moulded face sits 4pt inside it, which puts a second outline ABOVE the first as a ridge along the top edge. That is what the sell sheet's Cancel had. There is now **no `styleFrom` in `lib/ui` passing `side:` or `backgroundColor:` at all** — `architecture_test` fails the build on one — and the Play tab's own controls do not use Material buttons: the play button is its own six-layer chrome off `glass.css`, the dock is two orbs and a pill, and the quest block's are `StoreButton`s. So either this is a control the sweep does not cover or it is a different fault wearing the same description. One screenshot with the button in frame settles it)
[x] Match quests have ellipsis - no need for that.  (two changes: the redundant "TOTAL REWARD" label goes, and the heading wraps instead of being cut — "MISIONES DEL PARTIDO" still ran 23px over without it)


Shop
[x] THe top bar in is transparent - needs a background of some sort if only on the tabs as otherwise on things like turf and humbug they are hard to read (every tab is opaque; the selected one's shelf tint is blended onto the fill so the colour coding survives)
[x] the 3 premium ones have an icon on left then the text is like centered on the right hand side which looks odd, the icon needs to be either vertically centered on the left and same with text, or icon at top, followed by the title and then text then button (but needs to fit on the screen  (**the description really was centred, and the hero branch already thought it had said otherwise.** It wrapped every line in a `DefaultTextStyle.merge(textAlign: left)` — which does nothing, because a `Text` that sets its own `textAlign` beats the inherited one outright, and each of these lines sets `center` for the grid tile it shares the code with. So an offer ran a centred description under a left-aligned title. The alignment is picked per line now off `featured`. The icon was the other half and the spec settles it: `.shop-hero` is one `align-items:center` flex row, the port started it, so a 64pt picture hung off the top of a block three lines deep)
[x] the coin packs should have a border, the most popular one should have a diff one of course  (ALREADY THERE — a hairline in `kit.border`, and the popular one takes `kit.accentBright` with the accent glow under it. What was missing is anything holding it: no shop test asserted a border anywhere, and on turf and humbug an edge that quietly goes is a shelf of floating text. Pinned now, flagship against the rest)
[x] in fact all of these boosts & items also need a subtle border so they can be seen  (same — every boost and voucher is a `ShopTile` and its pane has carried `kit.border` since the tiles stopped being list rows. Pinned with the coin packs above)
[x] the gem buttons manager customisations are too small, they should be same size as rest of the buttons.  (it was `StoreButton(small: true)` — and `--sm` is the ROW variant, 11pt type in a 9pt radius, for a list line. Every other buy control in the shop is the full one. It comes with `stretch` off, which is the spec's own note on this pill: full width and it reads as the tile's footer bar rather than as the same button the gem rows use. The Owned pill beside it took the buy button's geometry too — a 999px capsule next to a moulded 10pt button is a different kind of control)
[x] All of the buttons in the manager customisations are see through which means the background shows. I dont want that, each should have a background, and look like they are on top of the screenl, like the rest.  (**the pack tiles had no surface at all** — the pane was the pack's own colour at 10% alpha over whatever the page was, so on turf and humbug the backdrop came through all ten. `.look-tile` is TWO stacked gradients: that faint tint wash over an opaque `surface-2 → surface`, the base every other tile in the shop is built on, and the port had ported only the wash. One `BoxDecoration` takes one gradient, so the tint composites onto the surface here instead. The drop shadow is the other half of "on top of the screen" and is in the spec too)

