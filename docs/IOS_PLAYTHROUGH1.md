penalty training game:
[x] keepers arms weird when he is standing there — the elbow bowed toward his FEET, which for an arm hanging down-and-out resolves INWARD: it sat 0.1 units off his centreline inside the 0.30-wide torso stroke, so the upper arm vanished into his chest and the forearm surfaced out of his ribs. It bows to the arm's own side now: elbows out, gloves in.
[x] the player running ups legs kinda cross, looks weird — the BOOTS were already pulled onto their own sides; the KNEES were not. A two-bone solve pinning both bones' screen lengths put the knee a third of a metre out to the wrong side (plant knee +0.04 with its boot at -0.10, kick knee -0.18 with its boot at +0.10) and the two swapped over at mid-run. A leg folds in DEPTH, so from behind it foreshortens — the knee sits at the thigh's share of the line with a small outward bow.
[x] the ball should return for the next goal within a second or two after scoring instead of having to wait for the user to click (which is never communciated) — the hold was always 1.9s; the frame it expires is the frame the kick is dropped and the mesh reset, and the ticker then returned WITHOUT a setState because it had just made itself idle. So the last thing painted was the ball in the net and the next repaint came from the player's own touch. It paints, then stops.
[x] the goalkeeper should randomally choose where he is going to dive, and if any part of his body hits the ball it should be a save, as we go up the leagues, the chances of him diving the right way increases but where he actually goes is still quite random. — already all three: `planKeeper` rolls `readChance` and falls back to a uniform `guess` for side, height and commit time; `_keeperGotIt` tests a capsule from his middle to his gloves (`keeperBodyRadius`), so a ball off his chest, hip or trailing leg is a save; and `keeperSmartChanceFor` ramps 5% at Sunday League to 30% at the Champions Cup.
[x] can we remodel the ball, doesn't really look like a football, can tell its generated — it was six flat pentagons stamped on a disc, full-sized right at the rim where a ball's panels are slivers, and the whole set spun as one rigid picture. It is the twelve pentagon centres of a real ball now (an icosahedron's vertices), each drawn as a cap ON the sphere and projected — so panels foreshorten into the limb, go round the back, and take the light the sphere is lit by.

training
[x] the skip cooldown is meant to be for all training not individually, put it at the top or bottom above or below all training and ensure clicking it skips all cooldowns. — one `_SkipAll` above the seven rows, shown only while something is actually resting, and it clears every kind. The engine already meant this: `skipKinds` is documented as "every kind one skip clears" and had no caller, alongside `resetMiniGameCooldown`, `recordSkipAd`, and `minigame.skip_all_ad` / `minigame.skip_all_left`, both shipped in ten catalogues and printed nowhere. The old per-row button was also wired to `onTap: null` — it does the skip now, bounded by the same three-a-day cap the rewarded ad will hang off.

squad:
[x] when clicking new trait, both spinniners should spin fully justl ike they do the first time, currently the second/third time, they hardly spin, they just kind amove to the newn one — `animateToItem` takes an ABSOLUTE index and the target was written as one (`pool.length * _revolutions + landing`), so it was three laps from where the reel STARTED and therefore three laps on the first roll only; after that the reel was already parked out there and the next roll asked it for the few rows between the old trait and the new one. `_reelTarget` measures from where the reel IS — full laps plus the residue, always forward — on both reels.

shop
[x] speical offers look good apart from, the coins should use the same yellow background we have in the hud for coins, and smae for energy etc, so darker background with ligher foreground - do this for all those small badges) — the pack-contents chips were a wash of the wallet's hue over `kit.surface2` with the vivid hue printed on it, so the GROUND was the page's and the coin chip was gold-on-near-white in light mode. They are filled with `hudBadgeColour` and printed in `hudBadgeInk` now, the same pair the HUD settled on. The three items that are not wallets (VIP clock, wardrobe, Director's crown) have no HUD face, so `packChipFace` takes them down until a lightened ink carries.
[x] the most popular banner goes across the premium text and isnt centered in the mdidle of teh banner. — two faults. The bar was placed by its TOP EDGE a fixed drop down the box and then rotated about its own centre, so where it landed depended on how tall the text rendered; measured, its centre sat 12pt up the band from the middle of the part actually visible (the chord across the corner, whose midpoint is the foot of the perpendicular from the corner). It is placed by that foot now. And the flash is a sibling of the pane while the hero's words run to the tile's edge, so it lay across the product name — the header row insets itself by `CornerBanner.clearance` when one is on, and the price line below it is left alone.
[x] the tabs look a bit rubbish, can we make them look better, for example hte top border doesnt respect the radius of teh tab below it — the accent was a 3pt bar in its own `ClipRRect` at the tab's radius, and an `RRect` scales its radii down to fit the box it is given: three points tall against a 12pt radius comes out at 3, so the bar's square ends stood proud of the rounded corner under them. Two more while there: `Border.all` is the only border a rounded `BoxDecoration` can have, so the selected tab drew a hairline across the BOTTOM — the mouth of the panel, which the broken baseline exists to keep open — and the unselected ones had a transparent border, so they were flat blocks of surface with no edge at all. `_TabFace` paints fill, accent and an OPEN edge (up one side, over the top, down the other) from one path.
[x] the gems and coin packs look rubbish, i dont like the best value badge or most popular badges on them, must prefer a banner. and the art work used isnt great. — both shelves' `ValueSeal` rosettes are `CornerBanner`s now, the same corner flash the offers hero wears, so a value claim is one device in this shop rather than two; the seal was a sticker sitting over the pile it recommended and it had shoved the crown across to the other corner to make room. `value_seal.dart` had no other caller and is gone. The art: the coin primitive was one flat fill of `coinGold` under a black rim, which is what reads as generated — it is struck metal now, lit off the top-left with a milled arc on the lit rim, and the edge-on coin a stack is built from is lit along its top. The gem primitive was already faceted and is untouched.
[x] the yellow text on the coin packs is yellow on white (in light mode) i think we need to use the same thing we do in the hud so its always visible — it was the raw `coinGold` (`#FFD700`, 1.1:1 on the near-white surface a light tile is built from), so the one line saying what the bundle actually pays was invisible. `coinFigureInk(context)` — money stays yellow, the shade moves, and the light value is the JS's own `--color-gold`.
[x] trophy polish, rather than having that for rest of season, lets do it for next 30 minutes, so youll have ot update text and update the spec of it. — **spec changed too**, since it is a mechanic: `boosts.trophyPolishSeason` becomes `boosts.trophyPolishUntil`, a deadline, in `coinSinkEngine.js`, `idleEngine.js`, `gemEngine.js` and `HUD.js`, and mirrored in `coin_sink_engine.dart`, `idle_engine.dart`, `income_breakdown.dart` and `gem_engine.dart`. `trophyPolishMultiplierFor` no longer takes a season at all, which retires the one place the five copies of this rule used to disagree. Copy retimed in all ten locale files and `gen_i18n.mjs` re-run (30 lines, the three keys per locale). An old save carrying a season stamp simply has no polish — at most half an hour of a buff it was going to lose at the next rollover.
    **And it is a HUD pill now**, which it could not be before: the boost row's sub is a countdown and a season has no minutes on it. Eight gems now buys something the player can watch running and watch go — added to `hud_boosts.dart` and to `HUD.js`'s `_renderBoosts`, needing no copy, off the catalogue's own 🏆.
[x] manager customisations, rather than listing the things each pack gives you under each other, put them next to each other with text under each one very similar to how it is on the customisation rig, sme for hair colour, we show a star, we should show the head with hair colour like we do on customisation rig. — the contents were a COLUMN of 54pt pictures with the name beside each and the rest of a wide sheet empty to the right, so the pictures were the smallest thing on the card. A `Wrap` of 64pt chips with the name under each, which is the customiser's own grid, and the tick moved into the chip's corner. And the colour axis was excluded from `_previewOf` alongside skin — but the customiser draws a SWATCH there and the shop has no swatch branch, so what a colour pack actually showed was `GameIcon('star')` once per colour. It draws the head now, off `_regionFor('color')`'s existing crop, with the hat forced to `none` — a manager in a bucket hat previewing four hair colours is four identical hats.

Game
[x] the commentary has too much margin at top and left and right, — a pass had already taken `feedInset` to nothing and made `feedBandInset` equal `matchInset` so the feed would line up with the tactic strip, and the ListView's own `fromLTRB(12, 0, 12, 12)` was left putting it straight back: a line still started 25 points in against the strip's 13, under six points of air the band above already paid for. The band's inset is paid once now; the bottom 12 stays, since it is what keeps the last line off the buttons.
[x] the 1x/2x subs and skip match buttons are just grey these should be the themed colours, possibily diff colours and make more sense — all three wore one `kit.surface2` face. `matchControlStyle` takes a face now and each one is what it is FOR: the speed toggle lights in the club's accent when 2× is on, so the one control whose colour is information carries it; Subs is `vsGreenPlate`, the same green a substitution is drawn in everywhere else; and Skip stays neutral on purpose — it is giving up on watching, and that should be the quiet one. Ink is picked by measured contrast, and it goes through `mouldedButtonStyle(face:)` because `backgroundColor:` on a moulded button fails silently.
[x] the arrow to indicate pressure, needs to move left and right a bit more to indicate pressure and it needs to make sense, if its pushing twoards them hard... then we should pretty much be on top, it gives us a chance to undrstand if the tactics we are using are working even if it doesnt necessarily mean a win. — three things. It barely moved: `dangerHome` is clamped 20–80 and the bias divided by 22, so full pressure wanted the very edge of that range while a real spell sits inside ±12 of level — 18 now, and `travel` 0.26 → 0.34, with the tip clamped so it presses to the six-yard box and no further. And it could not answer the tactics question at all: the chance weights come off the RATINGS and the counter lean alone, so switching High Press for Park the Bus moved possession twenty points and moved the arrow almost nothing. `dangerHome` is two thirds chances and one third territory now — where the ball is IS pressure, and it is the half a manager can change in the next ten seconds. Weighting the chances themselves is still off the table (it breaks 32 rows of `match_orchestration_parity_test`); this is the arrow's own figure and nothing else reads it.
[x] pause game when subs — already there: `openSubs` sets `_paused` before it awaits `showSubsPanel` and clears it after, and `_tick` returns while paused, so the clock, the feed and the sim all hold. Checked rather than changed.
[x] if hit post, the ball should bounce off it — it stopped dead on the point it arrived at and every figure froze around it. `_rebound` sends it back off the upright at 62% of the pace it arrived with, out on the side it struck and a little back up the pitch, clamped to stay on the grass; the rebound's own arrival is what then freezes the clip, so the defence plays it out.
[x] when keeper saves, they can either catch it or parry it away — the same dead stop. Which one is how hard it was hit, off `finishStyles`' own speeds: a header (100) or a rounded finish (120) is GATHERED, which is the existing stop and needs nothing drawn; a placed shot (150), a volley (175) or a long shot (200) is pushed away toward the touchline at a third of its pace. That is the split `penalty_physics.dart` makes at `_holdSpeed`, for the same reason.
[~] make some nice new cool scenarios that look good, can make dribbling, heading, volleying, whatever - be nice if the text matched up i.e. if the player shoots from distance, its a 'what a goal' or if he dribbles it through, we can highlight that, so its contextually aware of the type of goal
    **Scenarios: done.** Six new passages in both repos — a nutmeg and a shot on the turn, a corner headed BACK across the six-yard box and nodded in (two headers, which the set had none of), a scissor volley off a half-clearance, a keeper's throw breaking three men clear, a byline cutback met on the half-volley, and a centre-half striding out of his own half to hit one from thirty. The twenty-nine that were there are nearly all three or four beats ending in a shot from the same distance, so what these add is a MOMENT rather than more numbers.
    **The matching text is BLOCKED, and it is a copy block rather than a code one.** There is no style dimension anywhere in the catalogues: the goal pools are `commentary.goal.<equalise|extend|lead|pullback>.<with_scorer|no_scorer>` and the four variants in each are generic ("What a strike from {scorer}!", "Tucked away by {scorer}") in English and something else again in the other nine. Picking the variant by index would be contextual in English and arbitrary in every other language, which is worse than random. What this needs is `commentary.goal.<style>` pools written in `en.js` AND translated into the other nine — the catalogues are generated, so English-only would hand nine locales an English line in place of a translated one. Naming it here rather than faking it.

End Game
[x] I dont like that the match quests sometimes go on two lines with 'missed' next to them, i think we need to ensure they fit on one line - missed can go under if need be. — it was a `Row` of an `Expanded` ask allowed to wrap with the verdict beside it, so "Score inside 20 minutes" broke over two lines with "✕ Missed" parked against the first, three times over. A `Wrap` is exactly the rule asked for: the two run side by side while they fit and the verdict drops to its own run when they do not, so the block only grows on the sentences that need it. The ask is `scaleDown` rather than ellipsised — a report saying the player missed something without saying what is the fault the wrapping was added to fix. 
